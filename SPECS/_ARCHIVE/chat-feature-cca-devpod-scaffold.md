<!--
@id: chat-feature-cca-devpod-scaffold
@version: 1.0.0
@model: codex
-->
# Archive: feature-cca-devpod-scaffold

Summary:
- Implemented default-on DevPod scaffold in `cca init` with flags `--no-container`, `--init-git`, `--force`.
- Added docs: `DOCS/CCA_INIT.md` (flow matrix) and `DOCS/DEVPOD_REMOTE_PLAYBOOK.md` (portable playbook + quickstart).
- Updated root docs (`README.md`, `_ENTRYPOINT.md`, `DESIGN.md`, `REQUIREMENTS.md`).

Raw transcript: not captured in `_RAW/RAW.md` for this spec.
